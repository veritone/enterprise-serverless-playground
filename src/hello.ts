module.exports.handler = async (event) => {
    // TODO implement
    // TODO Make it so the lambdas don't end in -index. Please.
    return {
        statusCode: 200,
        body: JSON.stringify('Hello from Lambda!'),
    };
}
